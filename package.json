{
  "name": "diabetes-ai-agent",
  "version": "1.0.0",
  "description": "A diabetes assistance to help with your day to day diabetes related tasks",
  "main": "index.js",
  "scripts": {
    "frontend:dev": "cd frontend && npm run dev",
    "backend:dev": "cd backend && npm run dev",
    "dev": "concurrently \"npm run frontend:dev\" \"npm run backend:dev\"",
    "frontend:build": "cd frontend && npm run build",
    "backend:build": "cd backend && npm run build",
    "build": "npm run backend:build && npm run frontend:build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "refresh-peloton-token": "ts-node backend/src/tools/peloton/refreshToken.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/openid-client": "^3.1.6",
    "concurrently": "^8.2.2"
  }
}
